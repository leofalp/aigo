# ============================================================================
# AIGO - AI Go Framework - Environment Variables Configuration
# ============================================================================
# This file contains all environment variables used by the AIGO framework.
# Copy this file to .env and fill in your actual values.
# ============================================================================

# ----------------------------------------------------------------------------
# OpenAI-Compatible API Configuration (Required for most examples)
# ----------------------------------------------------------------------------

# Your API key for OpenAI-compatible provider
OPENAI_API_KEY=your-api-key-here

# Base URL for the API endpoint
OPENAI_API_BASE_URL=https://api.openai.com/v1

# ----------------------------------------------------------------------------
# Default Model Configuration (Optional)
# ----------------------------------------------------------------------------

# Default LLM model identifier used when not explicitly specified
# The model name format depends on your provider
# Check your provider's documentation for available models
AIGO_DEFAULT_LLM_MODEL=your-model-name

# ----------------------------------------------------------------------------
# Model Cost Tracking (Optional)
# ----------------------------------------------------------------------------
# Configure model pricing for automatic cost tracking
# Costs are in USD per million tokens
# If not set, WithModelCost() option must be used in code
# Check your provider's pricing page for accurate values

# Cost per 1 million input tokens (USD)
AIGO_MODEL_INPUT_COST_PER_MILLION=0.150

# Cost per 1 million output tokens (USD)
AIGO_MODEL_OUTPUT_COST_PER_MILLION=0.600

# Cost per 1 million cached input tokens (USD) - Optional
# Only applicable if your model/provider supports prompt caching
AIGO_MODEL_CACHED_COST_PER_MILLION=0.075

# Cost per 1 million reasoning tokens (USD) - Optional
# Only applicable for models with chain-of-thought reasoning
AIGO_MODEL_REASONING_COST_PER_MILLION=5.00

# ----------------------------------------------------------------------------
# Compute/Infrastructure Cost Tracking (Optional)
# ----------------------------------------------------------------------------
# Cost per second of execution time (USD)
# Use this to track infrastructure costs (VMs, containers, serverless)
#
# Reference values:
#   AWS Lambda 128MB:  0.0000021  (~$5.50/month)
#   AWS Lambda 1GB:    0.0000167  (~$44/month)
#   AWS EC2 t3.micro:  0.0000028  (~$7.60/month)
#   AWS EC2 t3.small:  0.0000056  (~$15/month)
#   AWS EC2 t3.medium: 0.0001111  (~$30/month)
#
# To calculate: Monthly cost ÷ 2,628,000 seconds
AIGO_COMPUTE_COST_PER_SECOND=0.0000056

# ----------------------------------------------------------------------------
# Observability & Logging (Optional)
# ----------------------------------------------------------------------------

# Log output format
# Values: compact | pretty | json
#   - compact: Single line with JSON (default, production-friendly)
#   - pretty:  Multi-line with emoji and colors (development-friendly)
#   - json:    Standard JSON for log aggregation systems
AIGO_LOG_FORMAT=compact

# Log level - controls verbosity
# Values: TRACE | DEBUG | INFO | WARN | ERROR
#   - TRACE: Protocol details (HTTP requests, raw payloads) - very verbose
#   - DEBUG: Technical debugging (spans, memory ops, token usage)
#   - INFO:  Operational summaries (start/completion, status) - recommended
#   - WARN:  Warnings and potential issues
#   - ERROR: Errors only
AIGO_LOG_LEVEL=INFO

# Alternative log level variable (fallback if AIGO_LOG_LEVEL not set)
# LOG_LEVEL=INFO

# Alternative log format variable (fallback if AIGO_LOG_FORMAT not set)
# LOG_FORMAT=compact

# ----------------------------------------------------------------------------
# Tool-Specific API Keys (Optional - only if using these tools)
# ----------------------------------------------------------------------------

# Brave Search API Key
# Required for: bravesearch tool
# Get your key from: https://brave.com/search/api/
# Free tier: 2,000 queries/month
BRAVE_SEARCH_API_KEY=your-brave-search-key-here

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# 1. Variables prefixed with AIGO_ are framework-specific
# 2. Generic variables (LOG_LEVEL, LOG_FORMAT) are used as fallbacks
# 3. Cost tracking variables are optional - if not set, costs won't be tracked
# 4. Tool-specific keys are only required when using those specific tools
# 5. Never commit your .env file with real API keys to version control
# 6. See documentation for more details: https://github.com/leofalp/aigo
